# Full-res images (715MB) — too large for GitHub
ahrchives/

# Self-contained shareable HTML (~23MB each, images embedded)
ahrchives-mosaic-share.html
share-upload/

# Diagnostic / download scripts (not needed for the visualizer)
diag.mjs
diag2.mjs
download_ahrchives.mjs
download_ahrchives.py
download_opensea.mjs

# macOS
.DS_Store

# local backups / editor state
*.bak
.claude/
.netlify/

# local iteration checkpoints (session backups, not source)
home.checkpoint-*.html

# reference screenshots with spaces in names (break netlify deploy; local-only)
assets/Screenshot*.png
assets/* *.png

# unrelated Electron project living in this folder — its node_modules once broke a push (141MB file, GitHub's 100MB limit)
TAH!PES/

# Full-res masters (698MB, 1080px PNG). The site ships zoom/ (512px, 54MB) instead — see below.
# Keep these locally: they are the source for regenerating zoom/, and they hash to the
# collection's on-chain image CID QmVJncJRjcDRz8ok9kEKpjvfStvuV4GMhnEfYoz7xwro8j.
collection#999/

# Original art folders — superseded by zoom/ for anything the website serves. Local-only.
assets/OGs/

# Mint metadata for IPFS — never read by the website, and the space in the folder name
# breaks netlify deploys (same reason as the screenshots above).
assets/jsonfiles_for_ipfs updated/

# local backups
*#backup.html
assets/explorer_backup.html
